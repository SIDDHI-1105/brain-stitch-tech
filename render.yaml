services:
  - type: web
    name: brain-stitch-tech
    env: static
    buildCommand: bun install && bun run build
    staticPublishPath: .output/public
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
